{
  "name": "hq-construction-website",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "check": "node scripts/check.mjs",
    "build": "node scripts/check.mjs && node scripts/build.mjs && node scripts/check-dist.mjs",
    "preview": "node scripts/serve.mjs"
  }
}
